#
# For AGL Compositor (wayland compositor using libweston)
#
# /run/udev/data
lxc.mount.entry = /run/udev/data run/udev/data none ro,bind,optional,create=dir

# Weston(host) socket used by wayland-backed (AGL compositor)
lxc.mount.entry = /run/platform/display/wayland-host-0 run/platform/display/wayland-host-0 none bind,optional,create=file

# GPU (OpenGLES)
lxc.cgroup.devices.allow = c 226:* rwm
lxc.mount.entry = /dev/dri dev/dri none bind,optional,create=dir

lxc.cgroup.devices.allow = c 10:* rwm
lxc.mount.entry = /dev/pvr_sync dev/pvr_sync none bind,optional,create=file

# Input
lxc.cgroup.devices.allow = c 13:* rwm
lxc.mount.entry = /dev/input dev/input none bind,optional,create=dir

